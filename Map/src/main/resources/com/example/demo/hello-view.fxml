<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.control.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.demo.HelloController">

    <!-- LEFT PANEL -->
    <left>
        <VBox spacing="15" style="-fx-padding: 15; -fx-background-color: #eaeaea;" prefWidth="200">

            <!-- Selection Panel Label -->
            <Label text="Selection Panel">
                <font>
                    <Font name="System Bold" size="16"/>
                </font>
            </Label>

            <!-- Search City -->
            <VBox spacing="5">
                <Label text="Search University:"/>
                <TextField fx:id="searchField"
                           promptText="Type to search..."
                           onAction="#onSearch"/>
            </VBox>

            <!-- Tip Label -->
            <Label text="Tip: Double-click map blocks"
                   style="-fx-font-size: 10; -fx-text-fill: gray;"/>

            <!-- Zoom Buttons -->
            <HBox spacing="10" alignment="CENTER">
                <Button text="Zoom +" onAction="#zoomIn"/>
                <Button text="Zoom -" onAction="#zoomOut"/>
            </HBox>

            <!-- GRAPH STATS BUTTON -->
            <Button text="📊 Graph Statistics"
                    onAction="#showGraphStats"
                    style="-fx-font-size: 13px; -fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-weight: bold;"/>

            <Separator />

            <Label text="Navigation Info"
                   style="-fx-font-size: 20px; -fx-font-weight: bold;" />

            <Label fx:id="sourceLabel"
                   text="Source: None"
                   style="-fx-font-size: 16px;" />

            <Label fx:id="destLabel"
                   text="Destination: None"
                   style="-fx-font-size: 16px;" />

            <Separator />

            <Label text="Shortest Distance"
                   style="-fx-font-size: 15px; -fx-font-weight: bold;" />

            <Label fx:id="distanceLabel"
                   text="— meters"
                   style="-fx-font-size: 16px;" />

            <Label text="Estimated Steps"
                   style="-fx-font-size: 15px; -fx-font-weight: bold;" />

            <Label fx:id="stepsLabel"
                   text="— steps"
                   style="-fx-font-size: 16px;" />

            <Separator />

        </VBox>
    </left>

    <!-- CENTER PANEL -->
    <center>
        <VBox alignment="CENTER" spacing="10" style="-fx-padding: 10;">
            <Label text="University Map: Navigation System">
                <font>
                    <Font name="System Bold" size="14"/>
                </font>
            </Label>
            <ScrollPane pannable="true" fitToWidth="true" fitToHeight="true">
                <Pane fx:id="mapPane"
                      prefHeight="650"
                      prefWidth="850"
                      style="-fx-border-color: black; -fx-border-width: 2;"/>
            </ScrollPane>
        </VBox>
    </center>

</BorderPane>
